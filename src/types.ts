export interface JsonPlaceholderUser {
  id: string;
  name: string;
  email: string;
}

export interface JsonPlaceholderTodo {
  title: string;
  completed: boolean;
  userId: number;
  id: number;
}
